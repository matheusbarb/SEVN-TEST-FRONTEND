import styles from "@/styles/header.module.css"

const Header = () => {
    return (
        <div className={styles.container}>
            <h1 className={styles.logo}>SEVN NEWS</h1>
        </div>
    )
}

export default Header